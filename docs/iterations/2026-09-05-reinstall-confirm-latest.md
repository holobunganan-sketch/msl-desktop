# 核对优化版并再次卸载重装 — 2026-09-05

用户明确要求保留数据，卸载后重装优化版，并确认实际使用的程序版本。

## 核对与执行

- 操作前正在运行的程序位于 Codex 的 LocalCache 映射目录。该程序及原安装目录程序的 SHA256 均匹配本轮优化版，未发现旧程序指纹。
- 项目运行源码没有晚于当前安装包的文件；安装包为本轮已验证产物，版本号仍是 `0.1.0`。
- 关闭明确识别的 MSL 实例后，备份 EXE、卸载器及 SQLite 主数据库/WAL/SHM 文件。备份仅作二进制复制，未输出数据正文或凭据。
- 运行保留数据的卸载器，exit 0；卸载后原 EXE 不存在，卸载注册项为 0。
- 使用优化版安装包重新安装，exit 0；安装目录为 `C:\Users\ZhouNan\AppData\Local\msl-desktop`。
- 桌面及开始菜单快捷方式重新生成，目标均为上述安装目录的 EXE，逐个核对目标文件 SHA256 匹配优化版。未找到名称匹配 MSL 的任务栏固定快捷方式文件。

## 验证

| Action | Expected | Observed | Evidence | Verdict |
|---|---|---|---|---|
| 卸载后核对 | EXE 与注册项移除 | EXE 不存在，注册项为 0 | 独立卸载后检查 | PASS |
| 重装及入口核对 | 安装文件和快捷方式目标一致 | 安装器 exit 0；文件校验匹配 | SHA256 + 两个快捷方式目标 | PASS |
| 安装版基本界面 | 基本记录与导航可用 | 17/17 通过 | ui-smoke-cdp.py，CDP 9358 | PASS |
| 优化版特征核对 | 新增入口及后台存储存在 | 后台任务、关联文件夹、项目 AI 整理、收件箱归入项目、后台任务查询 5/5 通过 | 安装版 UI 与命令返回结构检查 | PASS |
| 数据保留 | 主数据库及日志无变化 | DB/WAL/SHM 均与关闭后备份一致 | 重装后及测试结束后文件校验 | PASS |

安装 EXE SHA256：`1AC5F0F047D04D728CE704DB8F82A94AAF0D6947AD2B483373E37101D116F219`。

正式数据库本次操作前后 SHA256：`738CA5C7ED123CCF295324E5ABD0B4E8A95341306EBFC7C38E968B248AA5414D`。

备份目录：`.test-runtime/reinstall-confirm-latest-20260905/backup/`。源工作文件、设置、凭据均保留；未读取真实 API Key，未调用真实 AI。应用启动和写入检查同时隔离 APPDATA、LOCALAPPDATA、TEMP、TMP；测试实例已关闭。

按照完成前验证技能执行上述检查。未把版本号字符串用作新版识别依据，采用文件指纹与新增功能检查确认。
