# MSL Desktop 0.1.2 · 项目整理请求失败修复

状态：应用端修复、验证和本机更新已完成。真实 Provider 的当前连通性未测试；本轮没有重新设计界面，上一轮的视觉审阅状态保持不变。

## 证据与诊断边界

- 用户截图：项目整理 #23 失败，`error sending request ... /zen/go/v1/chat/completions`，标记已自动重试。
- 在应用未运行时复制正式 SQLite 数据库及 WAL/SHM，仅在可丢弃副本中读取运行耗时、错误类别和模型路由元数据。没有读取凭据或输出工作正文。
- #23 耗时 120 秒；#21 耗时 121 秒；较早同类失败多为 60 秒。两项项目整理使用 OpenCode Go / MiMo-V2.5，模型启用、可用。
- 旧适配器固定每次请求 60 秒，任何发送错误都重试一次，并通过 `error.to_string()` 丢掉超时等具体类别。上述耗时与固定超时重试高度吻合。历史错误没有保留底层类别，无法追溯当时每次连接的所有细节。
- OpenCode 官方文档列出的 MiMo-V2.5 地址正是该地址，因此本次保留接口配置：[Go 官方文档](https://opencode.ai/docs/zh-cn/go#api-端点)。
- 当前系统代理已启用，127.0.0.1:7897 有监听；未修改系统代理、关闭证书验证或绕过代理。
- 使用本地 Mock 复现；没有调用真实 AI Provider。服务端历史 HTTP 500 与网络环境的当前实际可用性不能由这些测试保证。

## 01 · 失败回归

- Action：增加真实 HTTP 回环测试，覆盖等待超时、拒绝连接、401 错误正文和 65 秒慢响应。
- Expected：旧行为被测试捕获。
- Observed：三个快速测试均失败；65 秒慢响应测试在旧版失败，总耗时 125.29 秒。报错与截图相同。
- Evidence：`src-tauri/src/ai/adapters.rs` 中 `transport_*` 测试；本轮终端结果。
- Verdict：PASS，已复现。

## 02 · 请求层修复

- Action：完整生成预算调整至 180 秒，包含连接及服务错误自动重试；连接探测单次预算 20 秒，连接建立预算维持 12 秒。仅对连接建立失败及既有可重试 HTTP 状态有限重试；等待结果超时不重新发起生成。提供 NET_TIMEOUT、NET_CONNECT、NET_DNS、NET_TLS、NET_RESPONSE 等可定位类别与中文处理提示。HTTP 错误按状态给出动作，不输出服务端错误正文或带查询参数的请求 URL。
- Expected：慢分析可完成；失败原因可辨认；避免重复生成、错误信息泄漏和无限运行。
- Observed：三个快速回归通过；65 秒慢响应测试通过，只有一次请求。额外设置完整生成总预算，防止重试使单次生成超出 180 秒；JSON 格式修复的两次生成仍受各自预算约束。
- Evidence：本轮 `cargo test transport_ -- --include-ignored --nocapture`，4/4 通过。
- Verdict：PASS。最终完整构建另行验证。

## 03 · 完整验证与交付

- Action：完成检查、构建、回环模拟接口、实际 WebView 流转、重启、缓存安全和数据库副本测试。
- Expected：修复应用端等待与错误分类问题，保留业务流程和数据安全边界。
- Observed：下表通过；最终代码上的 65 秒专用测试再次通过。正式构建的后台项目整理在 65 秒后完成，切换页面未影响任务，建议进入待确认队列。401 和拒绝连接均结束为失败状态，具有具体处理提示，未写入半成品。
- Evidence：`scripts/ai-network-cdp.py`、`scripts/verify-network-install-cdp.py`、`.test-runtime/ai-network-20260905/artifacts/` 及本轮工具输出。
- Verdict：PASS（应用端）。外部网络及模型服务的实时状态未作验证。

| 检查 | 结果 |
| --- | --- |
| pnpm check | 0 errors / 0 warnings |
| pnpm build | PASS |
| cargo fmt --check | PASS |
| cargo test | 119 passed，2 项专用测试默认忽略并另行运行 |
| 65 秒慢响应专用测试 | 1/1 PASS，最终代码重复验证通过 |
| 正式数据库副本迁移专用测试 | 1/1 PASS |
| 通知回归 | 2/2 PASS |
| 基本 UI 操作 | 17/17 PASS |
| Mock 慢响应、切换页面、401、队列和失败恢复 | 10/10 PASS |
| 项目全链路、Mock AI、缓存安全 | 27/27 PASS |
| release 功能回归 | PASS |
| 11 个页面的两组字体/尺寸可达性 | 48/48 PASS |
| 安装并重启后的持久化 | 4/4 PASS |
| 安装后拒绝连接诊断和 UI 版本验证 | 2/2 PASS |
| pnpm tauri build | PASS，生成 0.1.2 NSIS 安装程序 |

所有应用运行、写入测试和构建均使用 `.test-runtime/ai-network-20260905` 内的 APPDATA、LOCALAPPDATA、TEMP、TMP。测试工作目录和提供商均为本地合成数据。

## 04 · 安装与数据保护

- Action：停止已验证路径的隔离测试进程，使用 NSIS `/S /UPDATE` 更新原安装目录；安装返回 0。再从已安装路径以隔离环境启动，检查 UI 版本、拒绝连接诊断及重启后的数据。结束时关闭测试进程和本地 Mock。
- Expected：桌面入口指向 0.1.2，原工作数据与模型配置保留。
- Observed：程序文件版本、注册表 DisplayVersion、界面版本均为 0.1.2。桌面快捷方式仍指向原安装路径，启动参数为空。正式数据库及 WAL/SHM 与本轮开始时对正式文件采样的哈希一致。
- Evidence：安装目录 `C:\Users\ZhouNan\AppData\Local\msl-desktop`；截图 `artifacts/installed-0.1.2.png`；安装包 SHA256 `8E8F6C2CF06705B6F1E30755F0A86F0AC926866288A5F253C10A696EEE9FD17F`。
- Verdict：PASS。

正式文件开始/结束哈希：

- DB：`AE8F7F2E59A50B1857DB473DD73D8EC8F4A5AAD88D92081429782D1A3851FC25`
- WAL：`D3510975321DCD488FBFA2018CE4C04F01A77AB4C6BDBC9F4AE1D94FB40CFD48`
- SHM：`01AE5E89F89FA96155EFEBB6099D299917347C28B57B4E49B8C50D9C94275816`

检查备注：SQLite 在打开可丢弃副本时更新了副本的 SHM 协调信息，因此结束时不能用该副本 SHM 的当前哈希替代开始时的采样值。首次比较发现这一差异后，已单独核对正式 SHM 仍与开始值一致，未执行任何恢复或覆盖操作。

已安装 EXE SHA256：`DD73CBB2FF34CB4454C9A2363D6550D3436661DE31DEC6EF49879DBA7C0AFF6A`。隔离应用及 Mock 进程已关闭，9360/9462 测试端口均无监听。

安装程序：`src-tauri/target/release/bundle/nsis/msl-desktop_0.1.2_x64-setup.exe`。

注意：`network-error-actionable.png` 中的 401 和初始未绑定路由均由隔离测试制造，不能用来判断用户真实 API Key 是否有效。历史错误记录保留，用户重新发起后会产生新的运行记录。
